const Weapon = function(weapon) {
    const typeOf = weapon.typeOf;

    if(typeOf === 'offensive')
    {
        this.damage = this.damage;
    }
    this.weaponType = weapon.typeOf
    this.description = this.description;
}

export default Weapon;